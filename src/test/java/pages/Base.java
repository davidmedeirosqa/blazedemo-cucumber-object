package pages; // Define o pacote onde a classe está localizada

import org.openqa.selenium.WebDriver; // Importa o WebDriver para controle do navegador

public class Base { // Classe simples que pode servir de base para outras, se for estendida

    public WebDriver driver; // Declaração do WebDriver (obs: nome ideal seria "driver" para manter padrão)
}
